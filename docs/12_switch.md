# 12 Switch

# Goal
Usw a switch to make an if statement easier and more readable

# Code
Look: `src/12_switch.cpp`

# Notes
- You can stack multiple conditions with less code using `case` and `break`
- `default:` = acts like `else`, will be executed if no condition is met
