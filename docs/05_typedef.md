# 05 Typedef & Using

# Goal
Use `typedef` & `using` to give an alias to a data type

# Code
Look: `src/05_typedef.cpp`

# Notes
- `typedef` = only useful when shortening a long data type
- `using` = is a better option
- generally these keywords are used for readability, less typos and comfort
