# 35 Function Templates

# Goal
Know what function templates are and how to use them

# Code
Look: `src/35_templates.cpp`

# Notes
- Used instead of making multiple overloaded functions
- Makes a function accept multiple datatypes
- `auto` = To make the function determine what datatype to return automatically
- `template<typename T>` = To create a template
