# 30 Credit Card Validator Program

# Goal
Making a working credit card validator program inside the console

# Code
Look: `src/30_creditcard_validator.cpp`

# Notes
- `%` = returns the remaining number, if it is not completely dividable by 10
- Using `string` instead of `int` can be useful when treating the input as an array of `char`
- iterate with for loops and use the algorithm for credit card validation
