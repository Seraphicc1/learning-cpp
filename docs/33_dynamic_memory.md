# 33 Dynamic Memory

# Goal
Understand dynamic memory

# Code
Look: `src/33_dynamic_memory.cpp`

# Notes
- `new` = to create dynamic memory which gets saved inside the Heap rather than the Stack
- `delete` = to delete it afterwards to avoid memory leak
- Best practice would be to use `vector<>` instead rather than manual memory management
