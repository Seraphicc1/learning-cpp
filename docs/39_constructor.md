# 39 (Overloaded) Constructor

# Goal
Implement constructors inside of your classes

# Code
Look: `src/39_constructor.cpp`

# Notes
- The `constructor` is automatically called when an object is created, making it possible to immediatly assign values as arguments
- Similar to `functions`, you can overload a constructor to make different arguments possible
