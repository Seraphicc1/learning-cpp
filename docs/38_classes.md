# 38 Classes & Objects

# Goal
The basics and principles of classes and object oriented programming

# Code
Look: `src/38_classes.cpp`

# Notes
- `class` = Create a class with attributes and functionality, e.g. Dog <- 10 years (int), golden retriever (string), Can bark (DoBark() function) and can sleep (GoSleep() function)
- "Dog Labrador1" = Create a unique instance of the `class`
- The `constructor` is automatically called when an object is created, making it possible to immediatly assign values as arguments