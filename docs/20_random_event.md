# 20 Random Event Generator

# Goal
Practice program to generate a random event

# Code
Look: `src/20_random_event.cpp`

# Notes
- Useful for games (e. g. random weather, random event)
- `switch` = Used to assign an event to each roll you can get
