# 37 Enums

# Goal
Know what an enum is and how to use it

# Code
Look: `src/37_enums.cpp`

# Notes
- `enums` = A data type made by the user that consists of multiple paired and named integer `constants`
- `enums` = Make it possible to use a switch for `strings`
- If you do not assign an `int` value to the `string` values it automatically assigns them starting at 0
