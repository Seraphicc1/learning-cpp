# 08 User Input

# Goal
Learn how to accept and use user input

# Code
Look: `src/08_user_input.cpp`

# Notes
- `cout <<` = output + insertion operator
- `cin >>` = input + extraction operator <-- used for simple user input
- `getline` = if user input could contain spaces
