#include <iostream>

int main() {

    int students = 20;

    //students = students (+-*/) x;
    //students (+-*/)= x;
    //students (++ --);

    int remainder = students % 3;

    std::cout << remainder;

    return 0;
}